var uiModifier = uiModifier || {};
uiModifier.uiBind = (render, element, scopeVars) => {
    for (let attributesKey in element.attributes) {
        if (element.attributes.hasOwnProperty(attributesKey)) {
            if (attributesKey.startsWith("bind-")) {
                $(element).
            }
        }
    }
};

